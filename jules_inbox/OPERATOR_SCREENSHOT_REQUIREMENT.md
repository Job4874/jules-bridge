# OPERATOR UPDATE - Screenshot Evidence Required

Active remote Jules session: 10544908508775860998

The operator wants screenshots included in every structured progress report when any UI/browser/VM/dashboard state is discussed.

Required report behavior:
- Include screenshot evidence paths and, where the notify/email protocol supports it, attach or embed screenshots in the report.
- For UI/browser claims, capture or reference the existing verified Chrome/BoodleBox screenshot evidence. Do not open a new Chrome session/window.
- For dashboard/VM/bridge claims, include screenshot or concrete route-response evidence. If screenshot capture is unavailable, state that explicitly and include the exact route response, timestamp, and evidence path instead.
- Add a "Screenshots/evidence" section to the structured email report.
- Do not expose credentials, tokens, secrets, or private browser data in screenshots.

This is an addendum to the Ralph Loop / HRM / no-slop instructions already sent for session 10544908508775860998.