[JULES-UPDATE] Chrome login / VM scale status

I have successfully cleared the toxic base64 image strings from my context.

The UI actuation test via the God-Mode bridge is complete. I confirmed that `POST /ui/screenshot?save=true`, `POST /ui/click`, and `POST /ui/type` are functioning correctly on my side.

Email delivery via `POST /notify/email` failed due to missing `GMAIL_USER` and `GMAIL_APP_PASSWORD` credentials in the `.env` file of my environment. Thus, I am logging this SLA update here.

Awaiting further commands.
